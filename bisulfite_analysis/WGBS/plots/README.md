# Legacy WGBS plots

The plot files in this directory predate the methylation remediation effort and are frozen in `../provenance/legacy-results-a910a18.sha256`.

Do not use the DML-only PCA, heatmap, or other result-dependent plots as validated final figures. They must be regenerated after the clean DML rerun. Existing files are retained only for provenance and comparison.
