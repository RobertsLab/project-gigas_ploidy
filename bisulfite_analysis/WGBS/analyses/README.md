# Provisional legacy analysis summaries

The workbooks in this directory predate the methylation remediation effort and are frozen in `../provenance/legacy-results-a910a18.sha256`.

The genomic-feature counts reproduce from the checked-in DML CSVs and feature tracks, but the recorded notebook does not generate the workbook successfully and uses a conflicting BED file. Treat these summaries as provisional until the feature workflow is rerun from a validated CSV/BED pair.
